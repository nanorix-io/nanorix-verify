cff-version: 1.2.0
message: >-
  If you use this verifier, or cite the Signed Containment Evidence
  specification it implements, please cite it using the metadata below.
title: >-
  Signed Containment Evidence: specification and reference verifiers
abstract: >-
  This is the specification and reference implementation set for Signed
  Containment Evidence, a document format for evidence that anyone can check.

  A regulated organisation running someone else's software has to show its
  regulator what that software did with its data, and normally the only record
  is one the operator produced about itself. Signed Containment Evidence is
  built so that the party the evidence concerns is not the party attesting to
  it, and so that checking it requires no relationship with, permission from, or
  network call to whoever issued it. An auditor, a regulator, a counterparty, or
  an opposing expert can reach the same verdict about the same document, offline,
  years later.

  The document records that a compute workload ran inside a sealed execution
  boundary and was then destroyed. It carries an eight-step SHA-512 hash chain
  over the destruction sequence, together with an Ed25519 attestation over a
  fixed canonical view of the document serialised under RFC 8785.

  A verifier answers two separate questions. Integrity is whether the document
  is internally consistent and unmodified, which can be established from the
  document alone. Authenticity is whether the key that signed it belongs to the
  party named in it, which requires an external trust anchor. A document can be
  internally consistent and still be signed by a key that nobody vouches for, so
  the two results are reported separately and the exit codes distinguish them.

  The repository contains four implementations, in Rust, Go, Python and
  TypeScript, along with a shared failure taxonomy and a conformance corpus of
  100 cases in nine categories. The corpus is produced by a generator that is
  included, so its expected verdicts can be regenerated and compared rather than
  taken on trust. All four implementations have a corpus runner in the
  repository and all four produce the expected verdict on every case. Rust and Go
  were written against the specification independently, which is the evidence
  that the document can be built from twice; Python and TypeScript are ports of
  the Rust implementation, so their agreement confirms the ports rather than the
  specification.

  Three properties hold by construction. None of the implementations has a
  network dependency, so offline verification follows from the code rather than
  from a policy. A verifier that does not implement a document's declared signing
  mode rejects the document instead of returning a partial result, which removes
  a downgrade an attacker could otherwise select. The wire form, meaning the
  eight steps and their order, the hash and signature algorithms, and the
  canonical view, does not change, because evidence retained for years must
  remain checkable by the algorithm that produced it.
type: software
authors:
  - family-names: Arra
    given-names: Chaitanya Kumar
    affiliation: Nanorix Inc.
    orcid: "https://orcid.org/0009-0000-9986-8683"
doi: 10.5281/zenodo.21901019
repository-code: "https://github.com/nanorix-io/nanorix-verify"
url: "https://nanorix.io"
license: Apache-2.0
# Keep `version` and `date-released` equal to the git tag this release carries.
# Zenodo mints one DOI per release and reads both fields from this file.
version: 1.1.0
date-released: "2026-08-19"
keywords:
  - audit evidence
  - tamper-evident logging
  - hash chain
  - Ed25519
  - RFC 8785
  - canonicalization
  - non-repudiation
  - agent security
  - regulated data
  - offline verification
